{
  "name": "@walmart/maybe-lazy-load",
  "version": "1.0.0",
  "description": "A configurable lazy component for React.",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://gecgithub01.walmart.com/electrode/maybe-lazy-load.git"
  },
  "bugs": {
    "url": "https://gecgithub01.walmart.com/electrode/maybe-lazy-load/issues"
  },
  "homepage": "https://gecgithub01.walmart.com/electrode/maybe-lazy-load",
  "scripts": {
    "demo": "builder run generate && builder run server-dev",
    "demo-iso": "builder run dev-iso",
    "generate": "npm run generate-metadata && npm run generate-documentation",
    "generate-documentation": "electrode-docgen --package ./package.json --src ./src --markdown components.md",
    "generate-metadata": "electrode-docgen --package ./package.json --src ./src --metadata components.json",
    "prepublish": "builder run npm:prepublish",
    "preversion": "builder run test",
    "test": "builder run check-cov"
  },
  "license": "UNLICENSED",
  "dependencies": {
  },
  "devDependencies": {
    "@walmart/electrode-archetype-react-component": "^10.5.0",
    "@walmart/electrode-archetype-react-component-dev": "^10.5.0",
    "@walmart/electrode-demo-index": "^1.4.0",
    "@walmart/electrode-docgen": "^1",
    "@walmart/lithe-extras": "^2.8.0",
    "builder": "^2.10.1",
    "react": "^0.14.8",
    "react-dom": "^0.14.8"
  },
  "engines": {
    "node": "^4.2.6",
    "npm": "^3.5.3"
  }
}
